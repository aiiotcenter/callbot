{
  "name": "callbot",
  "version": "1.0.0",
  "private": true,
  "description": "Secure Node.js callbot service for NEU hospital",
  "main": "src/index.js",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "admin:dev": "npm --prefix admin-dashboard run dev",
    "admin:build": "npm --prefix admin-dashboard run build",
    "test": "node --test",
    "lint": "node --check src/*.js"
  },
  "dependencies": {
    "@fastify/helmet": "^13.0.2",
    "@fastify/rate-limit": "^10.3.0",
    "@fastify/websocket": "^11.2.0",
    "dotenv": "^16.6.1",
    "fastify": "^5.6.1",
    "openai": "^6.3.0",
    "ws": "^8.18.3",
    "zod": "^4.1.5"
  }
}
